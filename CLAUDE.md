# CLAUDE.md

بورتفوليو شخصي لعبدالعزيز الحيدان (مهندس ذكاء اصطناعي / تعلّم آلة). عربي RTL
أولاً مع إنجليزية كاملة، ومساعد صوتي `Aime` مدمج. منشور على Vercel.

**وثيقتان حاكمتان، والأحدث تتقدّم عند التعارض:**
- `docs/REDESIGN-BRIEF.md` — التوجيه الحالي. يحكم التكوين واللون والمحتوى.
- `docs/DESIGN-SPEC.md` — المواصفة الأصلية. ما لم ينسخه التوجيه ما زال سارياً.

خالِف أياً منهما عند الضرورة، لكن اذكر السبب في الكوميت.

## Stack
- React 19.2 + Vite 8 — JavaScript فقط، لا TypeScript
- **صفر تبعيات وقت التشغيل غير `react` و`react-dom`** — التوجيه يمنع إضافة أي حزمة
- CSS خام بمتغيّرات — لا Tailwind، لا مكتبة UI، لا مكتبة حركة، لا راوتر خارجي
- ESLint 9 flat config

## الأوامر
- `npm run dev` · `npm run build` · `npm run preview` · `npm run lint`
- `node scripts/generate-tree.mjs > src/assets/neural-tree.svg` — يُعاد تشغيله
  فقط عند تغيير شكل الشجرة، والناتج يُودَع في الريبو
- **لا يوجد إطار اختبارات.** التحقّق = `lint` + `build` + قياس في المتصفح

## المجلدات
- `src/main.jsx` — نقطة الدخول؛ يعطّل استرجاع موضع التمرير
- `src/App.jsx` — التركيب والتوجيه
- `src/context/LanguageContext.jsx` — **كل** النصوص وبيانات المشاريع (EN + AR)
- `src/context/ThemeContext.jsx` — وضع الحبر/الورق
- `src/components/` — مكوّن لكل قسم، مع `.css` مجاور بنفس الاسم
- `src/lib/` — `router.js` · `Link.jsx` · `Bidi.jsx` (عزل ثنائي الاتجاه)
- `src/index.css` — رموز التصميم في `:root`، الـ reset، الأصناف المشتركة
- `scripts/generate-tree.mjs` — مولّد الشجرة، seed ثابت
- `docs/` — الوثيقتان الحاكمتان

## الأقسام
`hero` · `projects` · `now` · `contact` — **أربعة، ولا خامس.** «عني» سطر واحد في
الهيرو لا قسم، والمهارات والخبرة حُذفتا عمداً.

## الـ Conventions (مستنتجة من الكود)
- **مصدر واحد للنصوص**: لا نص ثابت في أي مكوّن. كل شيء من `useLanguage()` عبر
  `t.<section>.<key>`. كائنا `en` و`ar` مرآة — أي مفتاح يُضاف في الاثنين معاً.
- **البيانات محتوى لا كود**: المشاريع مصفوفة داخل `translations`.
- **الرموز حصراً**: `var(--…)` من `index.css`. صفر هيكس وصفر `rgba()` خام في أي
  مكوّن.
- **مادتان لونيتان**: حبر وذهب. `--mark` بحد **٣ مواضع في الشاشة الواحدة**؛
  ندرته هي قيمته. لا لون ثالث ولا حتى لترميز الحالات.
- **مستويا تباين لا ثالث**: `--text` حاضر بتباين كامل، `--absent` غائب وواعٍ بأنه
  خلفية. **لا نص جوهري في الطبقة الغائبة** — الغائب للزخرفة وحدها (ترتيب
  المشاريع، حدّ سطر «ما انكسر»). البنية من الحجم والمسافة لا من الإخفات.
- **ثلاثة مقاسات نصية**: `--t-body` و`--t-mid` و`--t-max` بنسب ١ : ٤ : ١٢، ولا
  شيء بينها. `--t-note` للتسميات المونوسبيس وحدها.
- **الخطوط**: `--font-display` (Amiri) للعناوين الكبيرة **فقط ولا ينزل تحت ٢×**
  لأنه يُقرأ تراثياً عند الصغر · `--font-body` (IBM Plex Sans Arabic) للمتن ·
  `--font-mono` (IBM Plex Mono) للأرقام والتسميات. تُحمَّل من `<link>` في
  `index.html` لا بـ`@import`.
- **المسافة تبني البنية لا الحدود**: صفر حدود وصفر بطاقات وصفر كبسولات. إن احتاج
  قسم حدّاً ليُفهم كوحدة فمسافاته غلط.
- **الحركة بالوزن**: `--t-fast` للخفيف، `--t-base`، `--t-heavy` للثقيل. تُكتب
  بخاصية صريحة، ولا تُحرَّك إلا `transform` و`opacity`. لا حركة دائمة في الصفحة.
- **خصائص منطقية حصراً**: `inset-inline-*`, `margin-inline-*`, `text-align: start`.
  لا `left`/`right` مطلقاً.
- **موبايل أولاً**: كل استعلامات الوسائط `min-width` عدا استعلام واحد في
  `NeuralTree.css` يخصّ قصّ الشجرة.
- **`<Bidi>`** يلفّ أي نص قد يحوي مصطلحاً لاتينياً أو رقماً داخل جملة عربية.
- **`data-target-id`** على البطاقات يطابق `id` في `translations`، وخادم آيم
  الخارجي يرجّع رموزاً مثل `projects.board-room` لتوجيه التمرير والإضاءة.
- **الأخطاء**: لا Error Boundary ولا مكتبة تنبيهات. فشل الشبكة يُبتلع بـ `catch`
  ويُعرَض كرسالة من `t.aime.*` بلغة المستخدم.

## لا تفعل
- لا تضف حزمة. التوجيه يمنعه، والتوجيه والحركة والاتجاه مكتوبة يدوياً لهذا السبب.
- لا تضف مفتاح ترجمة في `en` دون `ar` (والعكس) — يكسر الموقع عند التبديل.
- لا تغيّر `id` قسم ولا `id` مشروع — يكسر التنقّل وأوامر آيم القادمة من الخادم.
- لا تحرّك خاصية تخطيط (`width`, `padding`, `box-shadow`, `filter`).
- لا تستخدم `opacity: 0` كنقطة بداية لنص — يضرب LCP، ويجعل الصفحة بيضاء بلا
  حركة. لهذا لا يوجد ظهور بالسكرول أصلاً، والهيرو يستعمل `data-calibrate`.
- لا تُدخل حركة لا تنتهي. الشجرة تنمو مرة ثم تسكن، والصفحة تُقرأ ساكنة.
- لا تولّد الشجرة وقت التشغيل — seed ثابت وملف مُودَع، وإلا اختلف الشكل لكل زائر.
- لا تدع الشجرة تقع خلف نص. الهيرو عمودان في شبكة ليكون التداخل مستحيلاً.
- لا تحذف `data-mode` ولا سكربت الـ pre-paint في `index.html` — بدونه يومض الوضع
  الخطأ قبل أول رسم، ويسقط قرار التعاير.
- لا تلمس `vercel.json` — بدون الـ rewrite ترجع مسارات `/project/*` بـ 404 على
  النشر رغم أنها تعمل محلياً تحت `vite preview`.
- لا تعطّل `outline` على عنصر تفاعلي — القاعدة العامة في `index.css` تعطيه حلقة تركيز.
- لا تنقل منطق التمرير/الإضاءة خارج `AimeWidget` — هو المتحكّم الوحيد بالصفحة.
- عنوان خادم آيم ثابت في `AimeWidget.jsx` ويشير لمشروع منفصل.

اقرأ PROGRESS.md لآخر حالة عمل.
