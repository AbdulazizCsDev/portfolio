# CLAUDE.md

بورتفوليو شخصي لعبدالعزيز الحيدان (مهندس ذكاء اصطناعي / تعلّم آلة). عربي RTL
أولاً مع إنجليزية كاملة، ومساعد صوتي `Aime` مدمج. منشور على Vercel.

**`docs/DESIGN-SPEC.md` هو المرجع الحاكم للهوية البصرية.** أي تعديل على المظهر
أو الحركة أو بنية المحتوى يُقاس عليه. خالِفه عند الضرورة، لكن اذكر السبب.

## Stack
- React 19.2 + Vite 8 — JavaScript فقط، لا TypeScript
- **صفر تبعيات وقت التشغيل غير `react` و`react-dom`** — المواصفة تمنع إضافة أي حزمة
- CSS خام بمتغيّرات — لا Tailwind، لا مكتبة UI، لا مكتبة حركة، لا راوتر خارجي
- ESLint 9 flat config

## الأوامر
- `npm run dev` · `npm run build` · `npm run preview` · `npm run lint`
- **لا يوجد إطار اختبارات.** التحقّق = `lint` + `build` + فحص في المتصفح

## المجلدات
- `src/main.jsx` — نقطة الدخول؛ يعطّل استرجاع موضع التمرير
- `src/App.jsx` — التركيب، والتوجيه، ومراقب الظهور بالسكرول
- `src/context/LanguageContext.jsx` — **كل** النصوص وبيانات المشاريع (EN + AR)
- `src/context/ThemeContext.jsx` — وضع الحبر/الورق
- `src/components/` — مكوّن لكل قسم، مع `.css` مجاور بنفس الاسم
- `src/lib/` — `router.js` (توجيه) · `Link.jsx` · `Bidi.jsx` (عزل ثنائي الاتجاه) ·
  `tokens.js` (قراءة متغيّرات CSS للكانفس)
- `src/index.css` — رموز التصميم في `:root`، الـ reset، الأصناف المشتركة
- `docs/DESIGN-SPEC.md` — المواصفة الحاكمة

## الـ Conventions (مستنتجة من الكود)
- **مصدر واحد للنصوص**: لا نص ثابت في أي مكوّن. كل شيء من `useLanguage()` عبر
  `t.<section>.<key>`. كائنا `en` و`ar` مرآة لبعض — أي مفتاح يُضاف في الاثنين معاً.
- **البيانات محتوى لا كود**: المشاريع والمهارات مصفوفات داخل `translations`.
- **الرموز حصراً**: `var(--…)` من `index.css`. لا هيكس ولا `rgba()` خام في أي
  مكوّن. الكانفس يقرأ الرموز عبر `lib/tokens.js`، والاستثناء الوحيد المقصود قيمة
  احتياطية في `TraceField.jsx` تعمل لو لم تُحلّ الرموز بعد.
- **مادتان لونيتان فقط**: حبر وذهب. `--mark` (الذهب) للمسار الناجح وحده، بحد
  3–4 مواضع في الشاشة. لا لون ثالث ولا حتى لترميز الحالات.
- **الحركة بالوزن**: `--t-fast` للعناصر الخفيفة، `--t-base`، `--t-heavy` للثقيلة.
  تُكتب دائماً بخاصية صريحة، ولا تُحرَّك إلا `transform` و`opacity`.
- **خصائص منطقية حصراً**: `inset-inline-*`, `margin-inline-*`, `text-align: start`.
  لا `left`/`right` مطلقاً — الاتجاه يُعبَّر عنه مرة لا يُنسخ يدوياً.
- **موبايل أولاً**: كل استعلامات الوسائط `min-width`. القاعدة هي القياس الصغير.
- **`<Bidi>`** يلفّ أي نص قد يحوي مصطلحاً لاتينياً أو رقماً داخل جملة عربية.
- **مُعرّفات الأقسام عقد**: `hero/about/experience/projects/now/skills/contact`
  يستعملها الـ Navbar وآيم عبر `getElementById`.
- **`data-target-id`** على البطاقات يطابق `id` في `translations`، وخادم آيم
  الخارجي يرجّع رموزاً مثل `projects.board-room` لتوجيه التمرير والإضاءة.
- **الأخطاء**: لا Error Boundary ولا مكتبة تنبيهات. فشل الشبكة يُبتلع بـ `catch`
  ويُعرَض كرسالة من `t.aime.*` بلغة المستخدم.
- **الأيقونات**: SVG مضمّن كمكوّن صغير أعلى الملف الذي يستعمله.

## لا تفعل
- لا تضف حزمة. المواصفة تمنعه، والتوجيه والحركة والاتجاه كلها مكتوبة يدوياً لهذا السبب.
- لا تضف مفتاح ترجمة في `en` دون `ar` (والعكس) — يكسر الموقع عند التبديل.
- لا تغيّر `id` قسم ولا `id` مشروع — يكسر التنقّل وأوامر آيم القادمة من الخادم.
- لا تحرّك خاصية تخطيط (`width`, `padding`, `box-shadow`, `filter`) — `transform`
  و`opacity` فقط.
- لا تستخدم `opacity: 0` كنقطة بداية لنص فوق الطيّة — يضرب LCP ويخالف §5.
  الهيرو يستعمل `data-calibrate` لا `data-reveal` لهذا السبب.
- لا تحذف `data-mode` ولا سكربت الـ pre-paint في `index.html` — بدونه يومض الوضع
  الخطأ قبل أول رسم، ويسقط قرار التعاير.
- لا تلمس `vercel.json` — بدون الـ rewrite ترجع مسارات `/project/*` بـ 404 على
  النشر رغم أنها تعمل محلياً تحت `vite preview`.
- لا تعطّل `outline` على عنصر تفاعلي — القاعدة العامة في `index.css` تعطيه حلقة تركيز.
- لا تنقل منطق التمرير/الإضاءة خارج `AimeWidget` — هو المتحكّم الوحيد بالصفحة.
- عنوان خادم آيم ثابت في `AimeWidget.jsx` ويشير لمشروع منفصل.

اقرأ PROGRESS.md لآخر حالة عمل.
