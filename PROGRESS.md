# PROGRESS.md

الفرع: `redesign/ink-and-gold` · ٣١ كوميت فوق `main` عند `0f1a321` · **لا تدفع على `main`**
`npm run lint` ✅ · `npm run build` ✅ · شجرة العمل نظيفة · كل شيء مدفوع
معاينة Vercel تُبنى تلقائياً من كل دفعة على الفرع.

إعادة تصميم كاملة وفق `docs/DESIGN-SPEC.md` ثم `docs/REDESIGN-BRIEF.md`.
الوثيقة الثانية أحدث وتتقدّم عند التعارض.

## تم

**الأساس البصري**
- لوحة الحبر والذهب في `:root`، بقيمتين لنفس الصبغة حتى يصمد الذهب على الورق.
  الوضع في `data-mode` على `<html>`، يُختم قبل أول رسم بسكربت مضمّن في
  `index.html`، ويملكه `ThemeContext` بعدها.
- كل قيمة لونية خام استُخرجت إلى رموز: صفر هيكس وصفر `rgba()` في المكوّنات.
- خصائص منطقية بالكامل، فسقطت ست كتل تجاوز RTL كانت تكرّر التخطيط يدوياً.
- موبايل أولاً: كل استعلامات الوسائط `min-width` بلا استثناء.
- العربية RTL هي الافتراضي، والإنجليزية لغة ثانية كاملة.
- عزل ثنائي الاتجاه عبر `src/lib/Bidi.jsx` للمصطلحات اللاتينية داخل الجمل العربية.

**التكوين بعد التوجيه الجديد**
- حُذفت أقسام النبذة والخبرة والمهارات. الأقسام أربعة: `hero` · `projects` ·
  `now` · `contact`، و«عني» صار سطراً واحداً في الهيرو.
- حُذف كل حدّ وبطاقة وكبسولة وإيموجي، وشارة التوفّر والمؤشر المتحرك وأيقونة
  السكرول. المسافة وحدها تبني البنية.
- القياس النصي ثلاث درجات بنسب ١ : ٤ : ١٢ ولا شيء بينها.
- مستويا تباين لا ثالث: حاضر وغائب، والغائب للزخرفة وحدها.
- بطاقات المشاريع صارت صفوفاً بعرض الصفحة: الاسم ورقمه على جانب والنص على الآخر.
  ثلاثة مشاريع، وأربعة عناصر لكل واحد، وصفر وسوم وصفر أسطح قابلة للتوسيع.
- خطوط: Amiri للعناوين الكبيرة، IBM Plex Sans Arabic للمتن، IBM Plex Mono
  للأرقام والتسميات. تُحمَّل بـ`<link>` مع `preconnect`.
- الهيرو متوسّط أفقياً ورأسياً (`justify-items` + `text-align: center` على
  `.hero-text`، و`margin-inline: auto` على ما له عرض أقصى). كان ملاصقاً لحافة
  البداية بفراغ واسع على الجانب الآخر — بقيّة من زمن كانت الشجرة تحتل ذلك
  العمود، وسقط سببها بحذف الشجرة.
- **صفحة المشروع صارت تُقرأ كوثيقة تقنية لا كقصة**: عنوانها نزل من `--t-max`
  إلى `--t-mid` (كان يملأ الشاشة الأولى كملصق)، والأقسام صارت مرقّمة `01…06`
  بخطّ فاصل فوق كل واحد، ومتن كل قسم يُقسَّم إلى فقرات كل جملتين بدل كتلة
  واحدة، مع `text-wrap: pretty` للفقرات و`balance` للعناوين و`overflow-wrap`
  للمعرّفات الطويلة (`gpt-4o-mini`, `max_tokens`) التي كانت تدفع السطر عند
  320px.
- **حقلا «الفكرة» و«ما انكسر» صارا مُسمَّيين** في البطاقة وصفحة المشروع معاً،
  بنفس نمط عناوين الأقسام (مونوسبيس صغير). كانا فقرتين بلا اسم يقرأهما الزائر
  ليعرف أيّهما أيّ. التسمية عرض لا عنصر خامس — قاعدة §7 «أربعة عناصر للبطاقة»
  ما زالت قائمة.
- **روابط المستودعات والعروض المباشرة صارت بيانات لا نصاً**: حقلا `repo`
  و`live` لكل مشروع، ويُعرضان صفاً واحداً أعلى صفحة المشروع (العرض المباشر
  أولاً) بأيقونتين وسهم، بـ`target="_blank"` و`rel="noopener noreferrer"`.
  حُذف العنوان الحافي من متن قسم «الكود» وبقيت الرخصة. البطاقة لم تتغيّر —
  الروابط في صفحة المشروع وحدها التزاماً بـ§7. عناوين العرض المباشر
  مستخرَجة من `origin/main` (النسخة المنشورة السابقة):
  `ai-board-room-2wtb.onrender.com` · `aime-voice-assistant-rw2z.vercel.app` ·
  `abdulazizcsdev-agrocure-app.hf.space`.
- **إشارة قابلية الضغط**: `.btn-quiet` و«افتح المشروع» ورابط المستودع كانت
  نصاً حافياً؛ صار لها خط سفلي و`cursor: pointer` وسهم يتحرك عند التحويم،
  ونزول بكسل واحد عند الضغط لكل الأزرار. الخط السفلي بـ`box-shadow` لا
  `border` لأن التوجيه يمنع الحدود، ولأن `border` يزيح التخطيط.
- **أيقونة الوضع صارت شمساً وهلالاً** بدل القطرة والورقة، وحُذف الـ`title`
  الذي كان يُظهر «حبر»/«ورق» عند التحويم — مصطلحان لا يعنيان شيئاً لزائر لم
  يقرأ التوجيه. `aria-label` باقٍ لقارئ الشاشة. الأيقونات كلها في
  `src/lib/icons.jsx`، مرسومة بلا إيموجي وبلا حزمة.
- **وصف «مجلس الإدارة الذكي» صُحّح**: كان يقول إن المستشارين يقرؤون مستنداتك
  ويتداولون، فيقرأ كأنهم يبدؤون من تلقائهم. الصحيح أن **المستخدم يطرح قراراً**
  ثم يتناقش الثلاثة **فيما بينهم** حوله، والمستندات مصدر الأرقام لا المحرّك.
  صُحّح في اللغتين.
- حُذفت ماسة اللوقو من النافبار، ومعها CSS ميت (`.logo-bracket` و`.logo-text`
  لم يستعملهما أي JSX). أثر جانبي عولج: اللوقو كان أول عنصر في صف
  `space-between`، فبحذفه انتقلت أزرار النافبار للحافة المقابلة في RTL؛
  أُعيدت بـ`margin-inline-start: auto` على `.nav-actions`، ويُصفَّر فوق 768px
  حيث تتكفّل هوامش `.nav-links` التلقائية بالتوسيط.

**المحتوى**
- **كل نصوص الموقع مكتوبة.** لم يبقَ أي نص نائب: المواضع الـ62 التي كانت
  معلَّمة بـ`[[ … ]]` في `src/context/LanguageContext.jsx` معبّأة كلها في
  `en` و`ar` معاً — أرقام المشاريع وملاحظاتها، والملخّصات، وسطر «ما انكسر»،
  وستة أقسام تفصيلية لكل مشروع من الثلاثة. المصدر مستودعات المشاريع نفسها
  وسجلّ كوميتاتها، لا صياغة عامة. ثلاثة حقول منها ما زالت تنتظر صوت المالك،
  انظر «بانتظار المالك» أدناه.

**البنية**
- توجيه بمسارات حقيقية (`/project/<id>`) في 53 سطراً على History API
  (`lib/router.js` + `lib/Link.jsx`)، مع `vercel.json` يعيد كتابة كل مسار إلى
  `index.html`.
- تسلسل تعاير عند التحميل بدل شاشة افتتاحية حاجبة: الصفحة تصل كاملة ومزاحة
  قليلاً وتنضبط خلال 2200ms، مرة واحدة لكل جلسة عبر `sessionStorage`.
- وصولية: حلقة تركيز على كل عنصر تفاعلي، ورابط تخطٍّ، ومعالجة فيض عند 320px.
- العنصر البصري الواحد: حقل جسيمات WebGL (`ParticleField.jsx`) خلفية ثابتة
  للصفحة كلها. تفاصيله كاملة تحت «قرارات».

**قياسات محقّقة على الحالة الحالية** (Playwright + Chromium محلي، الوضعان)
- كثافة الحقل تتبع المساحة: **400** عقدة عند 1280×900، و**273** عند 768×1024،
  و**91** عند 375×700 — بعدّ رؤوس `drawArrays(POINTS)` فعلياً، مطابق للمتوقّع
  حسابياً في المقاسات الثلاثة.
- الحقل ينجرف بلا توقّف: لقطات للكانفس عند t=3s/6s/9s مختلفة بايتياً في كل زوج.
  مع `prefers-reduced-motion`: لقطتان بفارق 1.5s متطابقتان بايتياً (إطار واحد
  يُرسم، ولا حلقة تُجدول أصلاً في هذا المسار).
- اللمس لا يشغّل التفاعل والماوس يشغّله — على نفس المقاس 375×700: اللمس أبقى
  عدد رؤوس الخطوط ثابتاً (3412→3398، ضجيج)، والماوس رفعه (3409→3464).
- تصغير العرض من 1280×900 إلى 375×700 يقصّ الشبكة عند الحافة الجديدة ولا يضغطها
  لتملأ، وعدد العقد ينتقل 400→91 ثم يعود 400 عند التكبير.
- تذبذب ارتفاع بنمط شريط عنوان الجوال (700→660→700→655→700) أبقى العدد ثابتاً
  على 91 بلا أي إعادة بناء.
- الذهب ساكناً = صفر مواضع. تحت مؤشر الماوس تضيء **كل** عقدة داخل المدى، وعلى
  الجوال لا ذهب إطلاقاً لأن اللمس لا يحرّك المؤشر.
- كل عنصر نصي يعبر تباين 4.5:1 في الوضعين، عدا ترتيب المشاريع (٠١/٠٢/٠٣) وهو
  زخرفة مقصودة في الطبقة الغائبة.
- صفر تمرير أفقي عند 320 و768 و1280.
- النسخي لا ينزل تحت 2.12× من المتن عند أي عرض، والحد 2×.

## قيد العمل

**لا شيء.** لا دالة نصف مكتوبة، ولا ملف معلّق، ولا `TODO` في الكود، ولا فرق بين
الشجرة المحلية والبعيدة، و`lint` و`build` يمرّان.

آخر ما لُمس: سطح المشاريع في `src/components/ProjectPage.jsx` (دالة
`toParagraphs` التي تقسّم متن كل قسم إلى فقرات، والترويسة التي تعرض الحقلين
المُسمَّيين ورابط المستودع) و`src/components/Projects.jsx` (تسمية الحقلين على
البطاقة)، مع `src/lib/icons.jsx` المستحدث. الثلاثة مكتملة ومقيسة في المتصفح.

الخطوة التالية أدناه لا تُكمل عملاً متوقّفاً — تبدأ من نظيف.

## قرارات

### البنية والتوجيه
- **مسارات حقيقية بدل راوتر hash.** الاثنان صفر تبعيات. رُفض الـ hash لأن صفحات
  المشاريع أكثر ما يستحق المشاركة والفهرسة، وروابط `#/` تُفهرس بضعف. الكلفة
  الزائدة rewrite واحد في `vercel.json`.
- **رُفض `react-router`** — التوجيه يمنع أي حزمة جديدة، والمطلوب 53 سطراً.
- **لا اختبارات آلية.** مشروع واجهة عرض؛ التحقّق `lint` + `build` + قياس في
  المتصفح. القياس في المتصفح ليس اختيارياً: عدة أخطاء لم تظهر إلا فيه، منها
  رموز CSS ميتة كانت تُرسم بحجم موروث، وعنصر يهبط على الحافة الخطأ في RTL.

### حقل الجسيمات — `ParticleField.jsx`

كل ما في هذا المكوّن مقصود، وأكثره بطلب مالك صريح يخالف قاعدة عامة مكتوبة في
مكان آخر. اقرأ البند كاملاً قبل تعديل أي رقم فيه.

- **وجوده أصلاً مخالفة مقصودة.** `docs/REDESIGN-BRIEF.md` يقول «احذف قبل ما
  تبني»، وشبكة النقاط والخطوط أشهر خلفية مجانية على الإنترنت — وقد حُذفت من
  التصميم مرة لهذا السبب بالذات. المالك طلب لاحقاً إعادتها كحقل جسيمات WebGL
  بعد اعتراض صريح مسبق وإعادة تأكيد منه. التخفيف القائم: الحقل خلف كل المحتوى
  (`z-index: 0` في جذر `App.jsx`) لا طبقة داخل الهيرو.
- **المصدر منقول حرفياً** عن مصدر زوّده المالك («Particle Drift — Originkit»)،
  محوّلاً من TypeScript إلى JS فقط. نفس البذرة `rng(20260824)`، ونفس الاتجاه
  (سقوط عمودي)، ونفس منطق إضاءة الذهب الثنائي، وينجرف بلا نهاية ولا يتجمّد.
  **الانحرافات المأذونة، كلها بطلب لاحق بالاسم:**

  | الانحراف | القيمة | السبب |
  |---|---|---|
  | مصدر اللون | `--text` / `--mark` بدل `#FFFFFF` / `#FDFF00` | يتبع تبديل الوضع فوراً |
  | السرعة | `SPEED_MULT` ×0.4 بدل ×1 (سرعة 20 من 50) | طلب صريح |
  | الكثافة | مشتقّة من المساحة بدل 400 ثابتة | تراكم على الشاشات الصغيرة |
  | تغيّر المقاس | لا إعادة توزيع للمواضع | الحقل يُقصّ لا يُجَر |
  | التفاعل | ماوس فقط | الإصبع كان يترك ذهباً عالقاً |
  | الشفافية | `opacity: 0.35` على الكانفس | تباين النصوص |

- **حارسان أُضيفا خارج المصدر، وكلاهما غير مرئي في الاستخدام العادي**: إطار
  ساكن واحد تحت `prefers-reduced-motion` (وصولية)، وإيقاف الحلقة عند إخفاء
  التبويب عبر `visibilitychange` (بطارية — التبويب المخفي لا يعرض إطاراً على
  أي حال). لا يوجد ولا يلزم `IntersectionObserver`: الكانفس `position: fixed;
  inset: 0` فهو داخل نطاق الرؤية دائماً ما دام التبويب ظاهراً.

- **الكثافة معدّل لا عدد.** 400 عقدة ثابتة كانت تعني أن جوالاً بمساحة 262K px²
  يحمل نفس عدد عقد ديسكتوب بمساحة 1.15M px² — **٤.٤ ضعف** الكثافة لكل وحدة
  مساحة. الآن `NODES_PER_AREA` مقيسة على مرجع 1280×900، فالديسكتوب يخرج 400
  كما كان بالضبط.
  - **السقف 400 والأرضية 80.** السقف أداء لا ذوق: حلقة الروابط `O(n²)`، فـ400
    عقدة = 80 ألف مقارنة/إطار، وشاشة 4K بلا سقف تصل ~800 ألف.
  - **مدى الربط والتحويم يُضربان بـ`clamp(sqrt(area/refArea), 0.5, 1)`.** تثبيت
    الكثافة بالمساحة وحده يضبط *عدد* الجيران رياضياً (كثافة × πr²)؛ لكن مدى
    230px ثابتاً يغطي 61% من عرض شاشة 375px فتُقرأ الروابط كخطوط تعبر الصفحة لا
    كشبكة محلية. هذا الجزء قرار بصري، مفصول عن جزء الكثافة الحسابي.
  - **لا `@media` ولا عتبة عرض** — الدالة متصلة فلا قفزة عند رقم سحري. (هذا
    يقرّبنا من `docs/DESIGN-SPEC.md` §6 الذي يطلب نصف الكثافة تحت 700px.)

- **التأقلم يغيّر عدد العقد لا مواضعها** — التمييز جوهري. المصفوفات تُحجز مرة
  واحدة بسعة `MAX_DENSITY` ويتحرك `nCount` وحده؛ العقد الباقية لا تتزحزح
  بكسلاً، والزيادة تُولَّد ضمن الحدود الحالية والنقصان يقصّ من الذيل. صفر إعادة
  تخصيص وصفر `bufferData` متكرر.
  - **نطاق 10% + تبريد 500ms**: شريط عنوان متصفح الجوال يطوى وينبسط أثناء
    التمرير فيغيّر الارتفاع باستمرار؛ بدونهما تتذبذب قائمة العقد.
  - **الأثر المقبول**: بعد تصغير كبير أو تدوير الشاشة تبقى العقد الناجية في فضاء
    البكسل القديم، فجزء منها خارج الإطار ويعود تدريجياً بالالتفاف عند الحواف.
    هذا هو معنى «تقتص»؛ البديل — إعادة التوزيع للملء الفوري — هو السلوك المرفوض
    نفسه. **لم يُضف «تدوير معجَّل للعقد الخارجة»**؛ إن أزعج بطء الامتلاء بعد
    تدوير الشاشة فهو تعديل يُطلب بالاسم لا يُستنتج.

- **الشفافية بـ`opacity` لا `filter: blur()`.** رُفض الـ blur: يذيب توهّج
  التحويم إلى ضبابية، ويضيف تمريرة GPU لكل إطار. الـ opacity يخفّف كل بكسل
  بنفس النسبة بلا كلفة ولا تعديل في الشيدرات.
- **الألوان تُقرأ كل إطار بـ`getComputedStyle`** فتتبع تبديل الوضع بلا إعادة
  بناء سياق WebGL. لا جدول شفافيات منفصل بين الوضعين — `uRestAlpha` ثابت 0.4
  كما في المصدر؛ الفرق بين الحبر والورق يأتي من قيم `--text`/`--mark` نفسها.
- **التفاعل بالماوس وحده**: `track`/`onLeave` يخرجان إن كان
  `e.pointerType !== 'mouse'`. الإصبع لا يملك حالة تحويم، فاللمسة كانت تثبّت
  المؤشر عند نقطتها وتترك انفجاراً ذهبياً عالقاً هناك. فُضّل `pointerType` على
  `@media (hover: hover)` لأن الأخير يعطّل الماوس على لابتوب بشاشة لمس.
- **حدّ «٣ مواضع ذهب في الشاشة» لا يسري على هذا المكوّن.** كل عقدة داخل المدى
  تضيء كاملة، فقد تضيء عدة عقد معاً إن تجمّعت. استثناء صريح على هذا المكوّن
  وحده، ومكانه هنا بدل تعديل القاعدة العامة.

- **⚠ قيود رُفضت صراحة — لا تُعِدها بلا طلب**: تجميد الحركة بعد مدة
  (`SETTLE_MS`)، أو اتجاه انجراف مائل بدل العمودي، أو تخفيض الكثافة أو
  `MAX_LINES` تلقائياً، أو حصر الذهب بأقرب عقدة واحدة، أو إعادة توزيع المواضع
  عند تغيّر المقاس. أُضيفت هذه مرة بلا طلب فرُفضت كلها. أي رقم في هذا المكوّن
  يُغيَّر فقط إذا طُلب بالاسم — لا لتحسين الأداء ولا «الشعور».

### النوع واللون والحركة
- **النص الجوهري لا يسكن الطبقة الغائبة.** التطبيق الحرفي لقاعدة المستويين وضع
  عناوين الأقسام وروابط التنقّل وتسميات التواصل عند 1.47:1، وروابط التنقّل غير
  النشطة صارت شبه معدومة. القاعدة موجودة لقتل الرمادي المتوسط في المتن لا
  لإخفاء محتوى، فالهرمية من الحجم والمسافة.
- **سطر «ما انكسر» بتباين كامل** لا خافت كما تقول المواصفة الأصلية: هو أثمن سطر
  في الصفحة، والوصولية تتقدّم.
- **الظهور بالسكرول حُذف كلياً** لا خُفِّف. كان يبدأ العناصر عند `opacity: 0`،
  فتظهر الصفحة بيضاء تحت الطيّة إذا عُطّلت الحركة. الحركة المتبقّية: تسلسل
  التعاير مرة لكل جلسة، وانجراف حقل الجسيمات المستمر.
- **`--ease-settle` غُيِّر عن حرفية المواصفة** إلى `cubic-bezier(0.22, 1.28, 0.36, 1)`.
  المنحنى المكتوب فيها يعطي تجاوزاً 0.13% بينما نصّها يطلب 2–3%؛ الوثيقة تناقض
  نفسها، والقيمة الحالية تعطي 2.5%.
- **النسخي للعناوين الكبيرة وحدها** ولا ينزل تحت 2× من المتن: يُقرأ تراثياً عند
  الصغر. الهامش الحالي 2.12× عند 320px، أي أن تصغير المتن أو حدّ الـ clamp
  الأدنى يكسر القاعدة.
- **المونوسبيس من عائلة Plex** لا JetBrains: يجعل المتن والمونو عائلة واحدة بدل
  ثلاثة أقلام غير مترابطة.
- **الخطوط بـ`<link>` لا `@import`**: الأخير لا يبدأ الطلب حتى يُحلَّل الـ CSS،
  والوجه العرضي أكبر عنصر في الشاشة الأولى.
- **حُذفت النبضة الدائمة من زر آيم**: كانت الشيء الوحيد الذي لا يسكن في صفحة
  يُفترض أن تُقرأ ساكنة.

### سطح المشاريع
- **متن الأقسام يُقسَّم إلى فقرات وقت العرض لا في المحتوى.** كل قسم مكتوب كسلسلة
  واحدة، فيُقرأ ككتلة مقال. `toParagraphs` في `ProjectPage.jsx` تقسّم على نقطة
  متبوعة بمسافة وتجمّع كل جملتين في فقرة.
  - **رُفض تعديل المحتوى نفسه** (إدراج `\n\n` يدوياً في ٣٦ متناً × لغتين):
    تحرير يدوي واسع لنصّ لم يكتبه البناء، وخطر خطأ مطبعي في محتوى صحيح مقابل
    مكسب هو نفسه.
  - **رُفضت القسمة على طول الحروف**: تقطع منتصف الجملة.
  - القاعدة تعمل بالعربية والإنجليزية معاً لأن كلتيهما تستعمل النقطة نفسها،
    والكسور (`0.015`) والمعرّفات (`gpt-4o-mini`) سليمة لأنها بلا مسافة بعد
    النقطة. متن بلا نقاط يرجع كما هو.
- **عنوان صفحة المشروع `--t-mid` لا `--t-max`.** بمقاس العرض كان اسم من ثلاث
  كلمات يملأ الشاشة الأولى، فتفتح الصفحة كملصق لا كوثيقة مرجعية. ما زال خط
  العرض وفوق حدّ الـ2× الذي يفرضه التوجيه لهذا الوجه.
- **الأقسام مرقّمة `01…06` بخطّ فاصل فوق كل واحد** — الترقيم يجعل الصفحة تُمسح
  بالعين كوثيقة ذات أجزاء بدل نصّ متصل، وهو ما اشتكى منه المالك («تظهر كأنها
  قصص مب بناء تقني»).
- **رابط المستودع حقل `repo` لا نص في المتن.** كان العنوان مكتوباً داخل فقرة
  «الكود» فلا يُضغط ولا يُفتح. صار بياناً يُعرَض كرابط حقيقي بأيقونة وسهم.
  - **رُفض وضعه على البطاقة**: §7 يحدّ البطاقة بأربعة عناصر، والرابط ليس منها.
  - `target="_blank"` مع `rel="noopener noreferrer"` — الأخيرة تمنع الصفحة
    المفتوحة من الوصول إلى `window.opener`.
- **الحقلان مُسمَّيان** («الفكرة» و«ما انكسر») بنفس نمط عناوين الأقسام. كانا
  فقرتين بلا اسم يقرأهما الزائر ليعرف أيّهما أيّ. التسمية عرض لا محتوى، فقاعدة
  «أربعة عناصر للبطاقة» باقية.
- **إشارة الضغط خط سفلي بـ`box-shadow` لا `border`.** التوجيه يمنع الحدود
  والكبسولات، و`border` يزيح التخطيط عند التحويم. ومعه `cursor: pointer` وسهم
  يتحرك و`:active { translateY(1px) }` — كلها `transform` فلا خاصية تخطيط
  تُحرَّك.
- **أيقونة الوضع شمس وهلال، وبلا `title`.** التلميح كان يعرض «حبر»/«ورق»، وهما
  مصطلحا الوثيقة الحاكمة لا مفردتان يفهمهما زائر. `aria-label` باقٍ لقارئ
  الشاشة.
- **الأيقونات مضمّنة في `src/lib/icons.jsx`**: SVG مرسوم بنمط واحد، بلا إيموجي
  (ممنوع في الواجهة) وبلا حزمة أيقونات (التوجيه يمنع أي تبعية جديدة).
- **وصف «مجلس الإدارة الذكي» صُحّح في اللغتين**: كان يقول إن المستشارين يقرؤون
  مستنداتك ويتداولون، فيُقرأ كأنهم يبدؤون من تلقائهم. الصحيح أن المستخدم يطرح
  قراراً ثم يتناقشون فيما بينهم حوله، والمستندات مصدر الأرقام لا المحرّك.

## التالي

خطوتان تُنفَّذان بلا قرار من المالك. الأولى أولاً — صارت أوضح بعد أن صارت صفحات
المشاريع تحمل روابط تُشارَك.

**١. حفظ اللغة عبر تحميل الصفحة.** `src/context/LanguageContext.jsx` سطر 459:
`useState('ar')` بلا أي تخزين، فكل تحميل كامل يرجع للعربية — يشمل فتح رابط
مشروع مباشرة أو تحديث الصفحة. التنقّل داخل الموقع سليم لأنه على History API.

- اقرأ القيمة الأولية من `sessionStorage.getItem('lang')` وارجع لـ`'ar'` إن
  غابت أو كانت قيمة غير `'en'`/`'ar'`.
- اكتبها في `toggle` (أو في `useEffect` يراقب `lang`).
- لُفّ القراءة والكتابة بـ`try/catch`: الوصول للتخزين يرمي استثناءً في وضع
  التصفّح الخاص في بعض المتصفحات، ولا يجوز أن يُسقط الموقع.
- **`sessionStorage` لا `localStorage`**: التعاير يستعمل `sessionStorage` أصلاً،
  واللغة تفضيل جلسة لا تفضيل دائم — واختيارها مرة لا يجب أن يقرّر لغة الزائر
  بعد شهر.
- التحقّق: بدّل للإنجليزية، حدّث الصفحة، افتح `/project/aime` مباشرة — الثلاثة
  بالإنجليزية. ثم `npm run lint && npm run build`.

**٢. كتابة `README.md`.** ما زال قالب Vite الافتراضي حرفياً («React + Vite —
This template provides a minimal setup…») ولا يذكر المشروع إطلاقاً. المطلوب:
ما هو الموقع، والمكدّس (React 19 + Vite 8، صفر تبعيات وقت تشغيل غير `react`
و`react-dom`)، والأوامر الأربعة، وبنية المجلدات في سطرين، والإشارة إلى
`CLAUDE.md` و`docs/` كمرجع حاكم. بالإنجليزية — جمهوره من يقرأ المستودع.

## بانتظار المالك

بنود لا تُنفَّذ بلا قراره، مفصولة عمداً عن «التالي»:

- **ثلاثة حقول كُتبت استنباطاً من سجلّ الكوميتات لا من صوت المالك**: `hero.line`
  (مسودة: «أبني أنظمة نماذج لغوية ورؤية حاسوبية… وأكثر العمل يذهب إلى الجزء
  الذي يجب أن ترفض فيه الإجابة»)، و`broke` ×٣، و«ما جُرّب وفشل» ×٣. الوقائع
  دقيقة ومصدرها كوميتات المالك نفسه؛ الصياغة تحتاج صوته — وهي أثمن أسطر
  الصفحة.
- **أرقام المشاريع تقبل الاستبدال**: `7` (نداءات LLM للسؤال) · `1.5` (ثانية
  الصمت، وهي من المسار المحلي دائم الاستماع لا من واجهة الويب) · `0.015` (خطأ
  المعايرة). الثالث الأقوى؛ الأول والثاني اختيار البناء لا المالك.
- **لغة تسويقية في وصفَي «قيد البناء»** («جاهزاً للاستخدام المؤسسي الموثوق»).
  ليست من العبارات الممنوعة بالاسم في التوجيه لكنها بنبرته المرفوضة: تُعاد
  كتابتها أو تبقى.

## مشاكل معروفة

- **لا زر يعيد للهيرو في النافبار** بعد حذف اللوقو — الروابط الثلاثة
  (`projects` · `now` · `contact`) لا تشمله. مقبول لأن الصفحة قصيرة، ويُراجَع
  إن طالت.
- **اللغة لا تُحفظ عبر تحميل الصفحة** و**`README.md` قالب Vite الافتراضي** —
  الاثنان لهما خطوات منفّذة في «التالي» أعلاه، لا يحتاجان قراراً.
- **مفردات تنقّل آيم قديمة.** `VALID_ACTIONS` في `aime-voice-assistant/llm.py`
  ما زالت تعدّد `about` و`experience` و`skills` و`projects.lines`
  و`projects.forecast` — وكلها حُذفت في إعادة التصميم. الخادم قد يرجّع رمزاً
  لقسم غير موجود فلا يحدث شيء. الإصلاح في الريبو الآخر لا هنا.
- **ادّعاء Raspberry Pi / 73ms** موجود في سيرة المالك (`knowledge.txt` في ريبو
  آيم) وغير موجود في ريبو `AgriCure-App` إطلاقاً — الريبو الحالي خليفة ويب
  بـSupabase. نصوص الموقع لا تدّعيه الآن؛ إن كان النشر على الـPi حقيقياً فهو
  يستحق ذكره بمصدره.
- **المساعد يعتمد على خدمة خارجية** (`AimeWidget.jsx`، عنوان ثابت في الكود). إن
  سقطت يظهر نص اعتذار فقط، ولا يوجد وضع بديل. العنوان يستحق أن يصير `VITE_*`.
- **`AimeWidget.jsx` 576 سطراً** ولم يُراجَع ضمن إعادة التصميم إلا لونياً
  وحركياً؛ بنيته الداخلية كما كانت، وواجهته أثقل من بقية الصفحة.
- **نصوص ثابتة داخل مكوّنين، خلافاً لقاعدة «مصدر واحد للنصوص».** ثمانية مواضع
  في `AimeWidget.jsx` بنمط `lang === 'en' ? 'Stop' : 'إيقاف'` (تلميحات الأزرار،
  ونصّ المُدخَل، ورسالتا فشل الميكروفون)، و`title="Toggle language"` في
  `Navbar.jsx` بالإنجليزية دائماً حتى في الواجهة العربية. الإصلاح نقلها إلى
  `t.aime.*` و`t.nav.*` — مفتاح في `en` ومثله في `ar`. لم يُنفَّذ لأنه خارج ما
  طُلب، ولأن تلميح زر اللغة قد يُحذف أصلاً كما حُذف تلميح زر الوضع.
- **لا اختبارات** — أي انحدار يُكتشف يدوياً.
- **معاينة Vercel محمية بمصادقة**؛ تُفتح بحساب المالك فقط.
- **حقل الجسيمات لم يُقَس على جهاز جوال حقيقي.** كل التحقّق بـ Chromium headless
  محلي بـviewport محاكى، لا FPS فعلي على عتاد ضعيف. الحمل انخفض كثيراً (91 عقدة
  بدل 400 عند 375×700)، لكن راجع معدل الإطارات على جهاز فعلي؛ وإن كان ضعيفاً
  فالخيار يُعرض على المالك لا يُخفَّض تلقائياً.
