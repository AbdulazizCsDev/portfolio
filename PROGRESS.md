# PROGRESS.md

آخر تحديث: 2026-08-24 · الحالة: كل العمل مدموج في `main` في المستودعين، لا فروع مفتوحة.

## تم

**إعادة التموضع كمهندس ذكاء اصطناعي** (كان الموقع يقول "خريج علوم حاسب متخصص في الباك إند")
- Hero و About أُعيدت كتابتهما حول بناء وإطلاق أنظمة LLM/ML إنتاجية.
- المهارات أُعيد ترتيبها: الذكاء التوليدي ونماذج اللغة أولاً، ثم التعلّم العميق، ثم MLOps.
- أُضيف مشروعان غائبان: **AI Board Room** (شارة المركز الأول) و **AgriCure** (شارة قيادة فريق)، والمشاريع رُتّبت بالذكاء الاصطناعي أولاً، والمشاريع غير المرتبطة نُقلت إلى صف "More Projects".
- قسمان جديدان: **Experience** (خط زمني) و **Now Building** (بطاقات بحالة حيّة).
- تحديث `index.html`: عنوان ووصف ووسوم Open Graph.
- استُبدل ملف السيرة الذاتية في `public/` بنسخة مطابقة للـ CV الجديد.
- كل ما سبق مُترجم بالكامل عربي/إنجليزي.

**Aime يقود الصفحة**
- الـ backend يُرجع `action` مع كل رد؛ الواجهة تنتقل للقسم و**تُبرز البطاقة المقصودة** (تكبر وتتوهّج وتخفت أخواتها).
- ردود قصيرة "تُشير ولا تكرّر": Aime ممنوع من إعادة سرد ما يظهر على الشاشة، يعلّق بجملة ويسأل سؤال متابعة.
- **جولة سريعة** (Quick Tour): 7 محطات بسرد صوتي، تتوقف فور تدخّل الزائر.
- **ردود جاهزة** لزرَّي "المشاريع" و"تواصل" — بدون استدعاء النموذج أصلاً.
- **Job Fit Check**: يلصق مسؤول التوظيف وصفاً وظيفياً فيحصل على تقرير مهيكل (مطابقات + مشروع يثبت كل متطلب + فجوات بصدق).

**تحصين الـ backend**
- حدّ معدّل 20 طلب/5 دقائق لكل IP، وسقوف على حجم الرسالة والتاريخ، وتسجيل الأسئلة.
- قواعد ضد حقن التعليمات ومحاولات تغيير الهوية.
- **صوت احتياطي**: إذا فشل الصوت المستنسخ يُستخدم صوت ElevenLabs قياسي فلا يصمت المساعد.
- `knowledge.txt` أُعيدت كتابته بالكامل ليطابق الـ CV الجديد.

**إصلاحات بصرية**
- الشعار ثلاثي الأبعاد نزل إلى `z-index: 0` (كان يمرّ فوق البطاقات).
- عند فتح لوحة المحادثة تنزاح الصفحة جانباً وتتحوّل الشبكات إلى عمودين، فلا تغطّي اللوحة المحتوى.

## قيد العمل

لا شيء قيد التنفيذ داخل المستودع حالياً.
المشاريع المعروضة في قسم "Now Building" (تطوير Board Room و AgriCure للاستخدام الموثوق والسوق السعودي) عمل خارج هذا المستودع — إذا تغيّرت حالتها فحدِّث `now.items` في `LanguageContext.jsx` باللغتين.

## قرارات

- **النموذج مُوجِّه لا مُصمِّم.** لا يولّد Aime واجهة؛ يُرجع رمز تنقّل من قائمة بيضاء والواجهة ترسم مكوّنات جاهزة. يمنع هذا أي واجهة مهلوسة، ويجعل أسوأ فشل ممكن "صياغة غير موفّقة" لا "صفحة مكسورة".
- **الموقع يبقى قابلاً للتصفّح بالكامل بدون محادثة.** مسؤول التوظيف يمسح بعينه ولا يكتب، والزاحف (crawler) لا يحادث، والـ backend قد يكون بارداً — فالمحادثة طبقة فوق الموقع لا بوّابة إليه.
- **سقوط تلقائي على `detectIntent()` المحلي** إذا لم يصل `action`، حتى ينشر الطرفان بأي ترتيب دون كسر.
- **`knowledge.txt` يُحقن كاملاً** في الـ system prompt بلا embeddings — الملف صغير، فالتعقيد غير مبرَّر.
- **إخفاء شريط الإنجازات من الـ Hero** (قرار المستخدم): عرض الجائزة في الواجهة الأولى يجعلها تبدو سقفاً لا محطة. الإنجازات باقية على بطاقات المشاريع وفي About حيث تُقرأ كدليل في سياقه.
- **قاعدة "حوّل الضعف إلى فرصة" مُبقاة** في شخصية Aime، مع إضافة "اعترف بالفجوة بصدق أولاً".

## التالي

1. **اختبار سلوك Aime الحيّ** بأسئلة حقيقية بعد النشر — خصوصاً قاعدة "لا تكرّر ما على الشاشة". إن كرّر، تُشدَّد الأمثلة في `llm.py`.
2. **تخزين صوت الجولة مؤقتاً** — كل تشغيل للجولة = ~7 استدعاءات ElevenLabs.
3. **صوت احتياطي عربي**: الصوت الحالي "Adam" إنجليزي الطابع؛ يُغيَّر بمتغيّر البيئة `FALLBACK_VOICE_ID` دون لمس الكود.
4. **حذف الملفات الميتة**: `ThreeScene.jsx` و `src/assets/{hero.png,react.svg,vite.svg}` و `public/{icons.svg,favicon.svg}` — لا شيء يستوردها.
5. تقسيم حزمة Three.js أو تحميلها بكسل إذا صار حجم الحزمة مزعجاً.

## مشاكل معروفة

- **حدّ المعدّل لكل instance وليس عالمياً** — على Vercel كل lambda لها ذاكرتها، فالحد إرشادي لا صارم. يحتاج مخزناً مشتركاً (Redis/KV) إذا زاد الضغط.
- **`allow_origins=["*"]`** في الـ backend — أي موقع يستطيع استهلاك الـ API على حسابك. يُفضَّل تقييده بنطاق الموقع.
- **البرودة الأولى (cold start)** على Vercel تجعل أول رد بطيئاً بشكل ملحوظ.
- حزمة `three.module` ~508 KB (تتجاوز حد تحذير Vite).
- لا اختبارات آلية إطلاقاً؛ الاعتماد على `build` + `lint` + الفحص البصري.
