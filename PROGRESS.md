# PROGRESS.md

الفرع: `redesign/ink-and-gold` · مبني على `main` عند `0f1a321` · **لا تدفع على `main`**
`npm run lint` ✅ · `npm run build` ✅ · معاينة تُبنى تلقائياً من كل دفعة على الفرع

إعادة تصميم كاملة وفق `docs/DESIGN-SPEC.md` ثم `docs/REDESIGN-BRIEF.md`.
الوثيقة الثانية أحدث وتتقدّم عند التعارض.

## تم

**الأساس البصري**
- لوحة الحبر والذهب في `:root`، بقيمتين لنفس الصبغة حتى يصمد الذهب على الورق.
  الوضع في `data-mode` على `<html>`، يُختم قبل أول رسم بسكربت مضمّن في
  `index.html`، ويملكه `ThemeContext` بعدها.
- كل قيمة لونية خام استُخرجت إلى رموز: صفر هيكس وصفر `rgba()` في المكوّنات.
- خصائص منطقية بالكامل، فسقطت ست كتل تجاوز RTL كانت تكرّر التخطيط يدوياً.
- موبايل أولاً: كل استعلامات الوسائط `min-width` بلا استثناء الآن.
- العربية RTL هي الافتراضي، والإنجليزية لغة ثانية كاملة.
- عزل ثنائي الاتجاه عبر `src/lib/Bidi.jsx` للمصطلحات اللاتينية داخل الجمل العربية.

**بعد التوجيه الجديد**
- حُذفت أقسام النبذة والخبرة والمهارات. الأقسام أربعة: `hero` · `projects` ·
  `now` · `contact`، و«عني» صار سطراً واحداً في الهيرو.
- حُذف كل حدّ وبطاقة وكبسولة وإيموجي، وشارة التوفّر والمؤشر المتحرك وأيقونة
  السكرول. المسافة وحدها تبني البنية.
- القياس النصي ثلاث درجات بنسب ١ : ٤ : ١٢ ولا شيء بينها.
- مستويا تباين لا ثالث: حاضر وغائب، والغائب للزخرفة وحدها.
- بطاقات المشاريع صارت صفوفاً بعرض الصفحة: الاسم ورقمه على جانب والنص على الآخر.
  ثلاثة مشاريع، وأربعة عناصر لكل واحد، وصفر وسوم وصفر أسطح قابلة للتوسيع.
- خطوط: Amiri للعناوين الكبيرة، IBM Plex Sans Arabic للمتن، IBM Plex Mono
  للأرقام والتسميات. تُحمَّل بـ`<link>` مع `preconnect`.
- العنصر البصري الواحد صار حقل جسيمات WebGL (`ParticleField.jsx`) بدل
  الشجرة العصبية — قرار مالك صريح يخالف §«احذف قبل ما تبني»، انظر «قرارات».
- حُذفت ماسة اللوقو من النافبار بطلب المالك، ومعها CSS ميت كان باقياً
  (`.logo-bracket` و`.logo-text` لم يستعملهما أي JSX). أثر جانبي عولج:
  اللوقو كان أول عنصر في صف `space-between`، فبحذفه انتقلت أزرار النافبار إلى
  الحافة المقابلة؛ أُعيدت لمكانها بـ`margin-inline-start: auto` على
  `.nav-actions` (يُصفَّر فوق 768px حيث تتكفّل هوامش `.nav-links` بالتوسيط).
  **ملاحظة**: لم يعد في النافبار زر يعيد للهيرو — الروابط الثلاثة لا تشمله.

**البنية**
- توجيه بمسارات حقيقية (`/project/<id>`) في 53 سطراً على History API
  (`lib/router.js` + `lib/Link.jsx`)، مع `vercel.json` يعيد كتابة كل مسار إلى
  `index.html`.
- تسلسل تعاير عند التحميل بدل شاشة افتتاحية حاجبة: الصفحة تصل كاملة ومزاحة
  قليلاً وتنضبط خلال 2200ms، مرة واحدة لكل جلسة عبر `sessionStorage`.
- وصولية: حلقة تركيز على كل عنصر تفاعلي، ورابط تخطٍّ، ومعالجة فيض عند 320px.

**قياسات محقّقة على الحالة الحالية**
- الذهب ساكناً = صفر مواضع في حقل الجسيمات؛ تحت **مؤشر الماوس** كل عقدة داخل
  المدى (`HOVER_REACH` 180px على الديسكتوب، مضروباً بمعامل المساحة على الأصغر)
  تضيء ذهبياً كاملة — لا الأقرب فقط، بحسب مصدر المالك الحرفي. هذا يخالف حدّ
  «٣ مواضع ذهب في الشاشة» في القيم الساكنة؛ الاستثناء مقصود ومُوثّق تحت
  «قرارات». على الجوال لا يوجد ذهب إطلاقاً: اللمس لا يحرّك المؤشر.
- كثافة الحقل تتبع المساحة: 400 عقدة عند 1280×900، و273 عند 768×1024، و91 عند
  375×700 — متحقّق بعدّ رؤوس `drawArrays(POINTS)` فعلياً في المتصفح، ومطابق
  للمتوقّع حسابياً في المقاسات الثلاثة.
- كل عنصر نصي يعبر تباين 4.5:1 في الوضعين، عدا ترتيب المشاريع (٠١/٠٢/٠٣) وهو
  زخرفة مقصودة في الطبقة الغائبة.
- صفر تمرير أفقي عند 320 و768 و1280 (Playwright + Chromium المحلي، الوضعين).
- حقل الجسيمات ينجرف بلا توقّف — لا `SETTLE_MS` ولا تجميد بعد مدة، طبق الأصل
  عن مصدر المالك. تحقّق بمقارنة لقطات شاشة للكانفس عند t=3s/6s/9s (بعد انتهاء
  تسلسل التعاير) وكانت مختلفة بايتياً في كل زوج، أي حركة مستمرة فعلاً لا
  ساكنة. مع `prefers-reduced-motion`: إطار واحد يُرسم ولا `requestAnimationFrame`
  ثانٍ — تحقّق بلقطتين بفارق 1.5s متطابقتين بايتياً، وبقراءة الكود.
- توقّف الحلقة عند إخفاء التبويب (`visibilitychange`) فقط — لا يوجد
  `IntersectionObserver`؛ الكانفس ثابت بحجم الشاشة كاملة (`position: fixed;
  inset: 0`) فهو داخل نطاق الرؤية دائماً ما دام التبويب ظاهراً، فمراقبة تقاطع
  إضافية زائدة لا حارس فعلي.
- تصغير الشاشة (Playwright: تغيير viewport من 1280×900 إلى 375×700 بعد
  الاستقرار) لا يعيد توزيع مواضع الجسيمات: لقطة فورية بعد التصغير تُظهر الشبكة
  مقصوصة عند الحافة الجديدة لا مضغوطة لتملأها. تحقّق بصري، انظر «قرارات».
- النسخي لا ينزل تحت 2.12× من المتن عند أي عرض، والحد 2×.

## قيد العمل

**لا شيء.** شجرة العمل نظيفة، وكل ما في الفرع مدفوع، و`lint` و`build` يمرّان.
لا دالة نصف مكتوبة ولا ملف معلّق ولا `TODO` في الكود.

كل ما تبقّى من عمل هو محتوى لا برمجة، وموضعه محدّد في «التالي» أدناه:
`src/context/LanguageContext.jsx`، النصوص المعلَّمة بـ`[[ … ]]`. ابدأ من هناك.

## قرارات

- **مسارات حقيقية بدل راوتر hash.** الاثنان صفر تبعيات. رُفض الـ hash لأن صفحات
  المشاريع أكثر ما يستحق المشاركة والفهرسة، وروابط `#/` تُفهرس بضعف. الكلفة
  الزائدة rewrite واحد في `vercel.json`.
- **رُفض `react-router`** — التوجيه يمنع أي حزمة جديدة، والمطلوب 53 سطراً.
- **الخلفية القديمة (شبكة عقد وخطوط) حُذفت ثم عادت بقرار مالك صريح لاحق.**
  الحذف الأول قائم كما وثّقناه: شبكة النقاط والخطوط أشهر خلفية مجانية على
  الإنترنت. المالك طلب لاحقاً استبدال الشجرة العصبية بحقل جسيمات WebGL
  (`ParticleField.jsx`) وهو بالتعريف شبكة عُقد وخطوط — مخالفة مباشرة لبند
  «احذف قبل ما تبني» في `docs/REDESIGN-BRIEF.md` ولمعيار «اقرأها بلا حركة»
  في §6 اعتراض عليها صراحة قبل التنفيذ، وأعاد المالك التأكيد. التخفيف الوحيد
  المتبقّي فعلياً: الحقل خلف كل المحتوى (`z-index: 0` في جذر `App.jsx`) لا
  طبقة داخل الهيرو، فالهيرو رجع عموداً واحداً بعد سقوط سبب العمودين (تجنّب
  تداخل الشجرة مع النص). (محاولة تخفيف سابقة أضافت أيضاً تجميداً بعد 4 ثوانٍ
  وحصر الذهب بعقدة واحدة أقرب — المالك رفضهما صراحة، انظر البند التالي.)
- **مصدر `ParticleField.jsx` أُعيد نقله حرفياً بعد أن انحرف عن الأصل بلا إذن.**
  نسخة سابقة أضافت من تلقاء نفسها: تجميد الحركة بعد 4 ثوانٍ (`SETTLE_MS`)،
  اتجاه انجراف مائل 18° بدل السقوط العمودي، سرعة أبطأ بنحو 4.3×، كثافة أقل
  بنحو 3.3×، سقفاً أشد على `MAX_LINES`، وحصر إضاءة الذهب بأقرب عقدة واحدة بدل
  أي عقدة داخل مدى المؤشر. المالك رفض كل هذا: **"ليش ماتتحرك؟ ليش مب نفس اللي
  عطيتك بالضبط؟ بس عدل الالوان اللي قلت لهم"**. أُعيد لصق مصدره الأصلي
  («Particle Drift — Originkit») ونُقل حرفياً سطراً بسطر — نفس البذرة
  (`rng(20260824)`)، نفس السرعة والاتجاه (سقوط عمودي)، نفس الكثافة (400)،
  نفس منطق إضاءة الذهب الثنائي (أي عقدة ضمن 180px تضيء كاملة، لا الأقرب
  فقط)، بلا `SETTLE_MS` وبلا تجميد أبداً. **الانحراف المتعمَّد وقتها كان مصدر
  اللون فقط**: `#FFFFFF`/`#FDFF00` الثابتين في المصدر استُبدلا بقراءة حيّة
  لـ`--text`/`--mark` — وهو بالضبط ما طلبه المالك. (لاحقاً أضاف المالك نفسه
  طلبات صريحة أخرى — سرعة أبطأ، لا إعادة توزيع عند تصغير الشاشة — موثّقة في
  بنودها الخاصة أدناه؛ هذه ليست انحرافات صامتة كسابقتها لأن المالك طلبها
  بالاسم.) إضافتان غير مرئيتان في
  الاستخدام العادي أُبقيتا كحارسين لا كتحسين أسلوبي: إطار ساكن واحد تحت
  `prefers-reduced-motion` (وصولية)، وإيقاف الحلقة عند إخفاء التبويب
  (بطارية — تبويب مخفي لا يعرض إطارات على أي حال، فلا يغيّر شيئاً يراه
  الزائر). **هذا يعني أن حدّ «٣ مواضع ذهب في الشاشة» غير مضمون فعلياً تحت
  المؤشر** — قد تضيء عدة عقد قريبة من بعضها في آنٍ واحد إن تجمّعت تحت نطاق
  180px. هذا استثناء صريح طلبه المالك على مكوّنه هو، لا كسر غير مقصود
  للقاعدة، ومكانه موثّق هنا بدل تعديل القاعدة العامة في «الرموز حصراً» أعلاه.
  **لا تُعِد أياً من القيود المرفوضة أعلاه إلى هذا الملف دون سؤال المالك أولاً
  — رُفضت مرة صراحة بالفعل.**
- **حقل الجسيمات يقرأ الألوان من `--text`/`--mark` مباشرة لا من ثوابت.**
  `getComputedStyle` تُقرأ كل إطار فتتبع تبديل الوضع فوراً بلا إعادة بناء
  سياق WebGL. لا جدول شفافيات منفصل بين الوضعين — `uRestAlpha` ثابت 0.4 كما
  في المصدر الأصلي؛ الفرق بين الحبر والورق يأتي من اختلاف قيم `--text`/`--mark`
  نفسها بين الوضعين، لا من منطق إضافي في المكوّن.
- ~~لا تبديل كثافة عبر JS breakpoint؛ الكثافة ثابتة 400 على كل عرض~~ —
  **سقط بطلب مالك صريح لاحق** ("ما زالت تتراكم الروابط والعقد بالشاشة
  الصغيرة… وخلها تتأقلم مع حجم الشاشة"). البند التالي يحلّ محلّه.
- **الكثافة ومدى الربط يُشتقّان من مساحة العرض، لا من breakpoint.** 400 عقدة
  ثابتة كانت تعني أن جوالاً بمساحة 262K px² يحمل نفس عدد عقد ديسكتوب بمساحة
  1.15M px² — أي **٤.٤ ضعف** الكثافة لكل وحدة مساحة، وهي الكتلة التي اشتكى
  منها المالك. الآن 400 تُقرأ كمعدّل لا كعدد: `NODES_PER_AREA` مقيسة على مرجع
  1280×900، فالديسكتوب يخرج 400 بالضبط كما كان، و375×700 يخرج 91 (متحقّق
  عملياً، وكذلك 273 عند 768×1024). القرارات الداخلية:
  - **السقف 400 والأرضية 80.** السقف أداء لا ذوق: حلقة الروابط `O(n²)`، فـ400
    عقدة = 80 ألف مقارنة/إطار، وشاشة 4K بلا سقف تصل ~800 ألف.
  - **مدى الربط والتحويم يُضربان بـ`clamp(sqrt(area/refArea), 0.5, 1)`.** تثبيت
    الكثافة بالمساحة وحده يضبط *عدد* الجيران رياضياً (كثافة × πr²)، لكن مدى
    230px ثابتاً يغطي 61% من عرض شاشة 375px فتُقرأ الروابط كخطوط تعبر الصفحة
    لا كشبكة محلية. هذا الجزء قرار بصري صريح، مفصول عن جزء الكثافة الحسابي.
  - **لا `@media` ولا عتبة عرض**: الدالة متصلة، فلا قفزة عند رقم سحري.
  - يقرّبنا هذا من `docs/DESIGN-SPEC.md` §6 (نصف الكثافة تحت 700px) بعد أن
    كان الالتزام بحرفية المصدر يخالفه.
- **التأقلم يغيّر عدد العقد لا مواضعها** — تمييز جوهري عن الـ rescale المرفوض.
  المصفوفات تُحجز مرة واحدة بسعة `MAX_DENSITY`، ويتحرك `nCount` وحده؛ العقد
  الباقية لا تتزحزح بكسلاً واحداً، والزيادة تُولَّد ضمن الحدود الحالية والنقصان
  يقصّ من الذيل. صفر إعادة تخصيص وصفر `bufferData` متكرر.
  - **نطاق 10% + تبريد 500ms**: شريط عنوان متصفح الجوال يطوى/ينبسط أثناء
    التمرير فيغيّر الارتفاع باستمرار؛ بدون النطاق تتذبذب قائمة العقد كل إطار.
    متحقّق: تذبذب ارتفاع 700→660→700→655→700 أبقى العدد ثابتاً على 91 بلا أي
    إعادة بناء.
  - **الأثر المقبول**: بعد تصغير كبير (ديسكتوب→جوال) تبقى العقد الناجية بمواضعها
    في فضاء البكسل القديم، فجزء منها خارج الإطار المرئي ويعود تدريجياً بالالتفاف
    الطبيعي عند الحواف. هذا هو معنى «تقتص» الذي طلبه المالك؛ البديل (إعادة
    التوزيع للملء الفوري) هو بالضبط السلوك المرفوض. لم أُضف «تدوير مُعجَّل
    للعقد الخارجة» من تلقاء نفسي — إن أزعج المالك بطء الامتلاء بعد تدوير
    الشاشة فهو تعديل واعٍ منفصل يُطلب بالاسم.
- **تفاعل المؤشر للماوس وحده.** `track`/`onLeave` يخرجان إن كان
  `e.pointerType !== 'mouse'`. الإصبع لا يملك حالة تحويم: اللمسة كانت تثبّت
  المؤشر عند نقطة اللمس وتترك انفجاراً ذهبياً عالقاً هناك للأبد (ظاهر في لقطة
  المالك من جواله). فُضّل `pointerType` على `@media (hover: hover)` لأن الأخير
  يعطّل الماوس على لابتوب بشاشة لمس. متحقّق بسياقين على نفس المقاس 375×700:
  اللمس لا يغيّر عدد رؤوس الخطوط (3412→3398، ضجيج)، والماوس يرفعه
  (3409→3464).
- **`SPEED_MULT` خُفِّض من ×1 إلى ×0.4 (سرعة 20 من أصل 50) بطلب مالك صريح**
  ("قلل السرعة ل 20") — لا انحراف صامت هذه المرة، والقيمة مذكورة بالاسم في
  التعليق فوق الثابت وفي `CLAUDE.md` حتى لا تُقرأ لاحقاً كخطأ يُصحَّح تلقائياً
  عائداً لـ×1.
- **شفافية الكانفس 0.35 عبر CSS `opacity` لا `filter: blur()`** (كانت 0.55،
  ثم خُفِّضت بطلب ثانٍ) — طُلب تخفيف
  ظهور الخلفية لصالح تباين النصوص مع ترك الاختيار بين الاثنين. رُفض الـ blur:
  يُذيب توهّج التحويم حول المؤشر إلى ضبابية بدل خط واضح، ويضيف تمريرة GPU
  إضافية لكل إطار. الـ opacity يُخفّف كل بكسل بنفس النسبة بلا كلفة إضافية ولا
  تغيير في الشيدرات نفسها — قرار مكانه CSS بحت.
- **لا إعادة توزيع (rescale) لمواضع الجسيمات عند تغيّر أبعاد الكانفس.** كانت
  المواضع تُضرب بنسبة العرض/الارتفاع الجديد إلى القديم في كل إطار تتغيّر فيه
  `cw`/`ch` — يشمل تصغير نافذة المتصفح فعلياً، وأيضاً تغيّر ارتفاع العرض
  المرئي على الجوال كل مرة يطوى/ينبسط شريط عنوان المتصفح أثناء التمرير، أي
  عملياً يحدث كثيراً على الجوال لا فقط عند تغيير حجم فعلي. النتيجة: الحقل
  كله ينضغط ليملأ أي أبعاد جديدة، فيبدو وكأنه "يُجَر" مع الشاشة. حُذف السلوك:
  المواضع تبقى بمقياس البكسل الذي بُنيت فيه أول مرة، والشيدر يحوّلها لفراغ
  القص (`uSize` الحالي) كل إطار — فأي جزء خارج الحدود الجديدة يُقصّ بواسطة
  الـ GPU مجاناً بدل إعادة حسابه، والالتفاف عند الحافة (`EDGE`) يستعمل الحدود
  الحالية فيعيد تدوير الجسيمات ضمنها بشكل طبيعي («تتراكم عادي»، بتعبير
  المالك) لا قفزة مفاجئة.
- **الظهور بالسكرول حُذف كلياً** لا خُفِّف. كان يبدأ العناصر عند `opacity: 0`،
  فتظهر الصفحة بيضاء تحت الطيّة إذا عُطّلت الحركة. الحركة المتبقّية في الصفحة
  الآن: تسلسل التعاير مرة لكل جلسة، وانجراف حقل الجسيمات المستمر بلا نهاية
  (بقرار مالك صريح، انظر أعلاه) — لا حدثين منتهيين فقط كما كان الحال قبل عودة
  الحقل.
- **النص الجوهري لا يسكن الطبقة الغائبة.** التطبيق الحرفي لقاعدة المستويين وضع
  عناوين الأقسام وروابط التنقّل وتسميات التواصل عند 1.47:1، وروابط التنقّل غير
  النشطة صارت شبه معدومة. القاعدة موجودة لقتل الرمادي المتوسط في المتن لا
  لإخفاء محتوى، فالهرمية من الحجم والمسافة.
- **سطر «ما انكسر» بتباين كامل** لا خافت كما تقول المواصفة الأصلية: هو أثمن سطر
  في الصفحة، والوصولية تتقدّم.
- **`--ease-settle` غُيِّر عن حرفية المواصفة** إلى `cubic-bezier(0.22, 1.28, 0.36, 1)`.
  المنحنى المكتوب فيها يعطي تجاوزاً 0.13% بينما نصّها يطلب 2–3%؛ الوثيقة تناقض
  نفسها، والقيمة الحالية تعطي 2.5%.
- **النسخي للعناوين الكبيرة وحدها** ولا ينزل تحت 2× من المتن: يُقرأ تراثياً عند
  الصغر. الهامش الحالي 2.12× عند 320px، أي أن تصغير المتن أو حدّ الـ clamp
  الأدنى يكسر القاعدة.
- **المونوسبيس من عائلة Plex** لا JetBrains: يجعل المتن والمونو عائلة واحدة بدل
  ثلاثة أقلام غير مترابطة.
- **الخطوط بـ`<link>` لا `@import`**: الأخير لا يبدأ الطلب حتى يُحلَّل الـ CSS،
  والوجه العرضي أكبر عنصر في الشاشة الأولى.
- **حُذفت النبضة الدائمة من زر آيم**: كانت الشيء الوحيد الذي لا يسكن في صفحة
  يُفترض أن تُقرأ ساكنة.
- **لا اختبارات آلية.** مشروع واجهة عرض؛ التحقّق `lint` + `build` + قياس في
  المتصفح. القياس في المتصفح ليس اختيارياً: عدة أخطاء لم تظهر إلا فيه، منها
  رموز CSS ميتة كانت تُرسم بحجم موروث، وعنصر يهبط على الحافة الخطأ في RTL.

## التالي

**تعبئة المحتوى المتروك للمالك.** كل النصوص النائبة في
`src/context/LanguageContext.jsx` ومعلَّمة بـ`[[ … ]]` — **62 موضعاً**، كلها في
كائني `en` و`ar` معاً. الخطوة:

1. `hero.line` — سطر واحد يقول من هو وما الذي يصنعه. ممنوع «بجودة إنتاجية» و«من
   البداية إلى النهاية» وما شابههما؛ يُكتب كشرح لمهندس محترَم.
2. لكل مشروع من الثلاثة (`board-room`, `aime`, `agrocure`) في `projects.items`:
   - `number` — الرقم الأصعب أو الأغرب، لا الأكبر
   - `numberNote` — سطر يقول ماذا يقيس
   - `summary` — جملة أو جملتان عمّا يفعله المشروع، لا التقنيات فيه
   - `broke` — ما الذي لم ينجح وماذا تعلّم منه
   - `detail.sections[]` — ستة أقسام: المعمارية، القرارات، المقاييس، الحدود،
     ما جُرّب وفشل، الكود
3. أي نص يُكتب في `en` يُكتب في `ar`. البنيتان مرآة، والنقص يكسر زر التبديل.
4. بعد التعبئة: `npm run lint && npm run build`، ثم فحص الرئيسية وصفحة مشروع
   واحدة على الأقل في الوضعين وعند 320px.

المواضع محدّدة والشروط مكتوبة داخل النص النائب نفسه، فلا حاجة لقرار إضافي.

## مشاكل معروفة

- **وصفا مشروعَي «قيد البناء» بلغة تسويقية** («جاهزاً للاستخدام المؤسسي
  الموثوق»). ليست من العبارات الممنوعة بالاسم في التوجيه، لكنها بنفس النبرة
  التي يرفضها. تحتاج قرار المالك: تُعاد كتابتها أو تبقى.
- **`README.md` ما زال قالب Vite الافتراضي** ولا يصف المشروع.
- **المساعد يعتمد على خدمة خارجية** (`AimeWidget.jsx`، عنوان ثابت في الكود). إن
  سقطت يظهر نص اعتذار فقط، ولا يوجد وضع بديل. العنوان يستحق أن يصير `VITE_*`.
- **`AimeWidget.jsx` 576 سطراً** ولم يُراجَع ضمن إعادة التصميم إلا لونياً
  وحركياً؛ بنيته الداخلية كما كانت، وواجهته أثقل من بقية الصفحة.
- **لا اختبارات** — أي انحدار يُكتشف يدوياً.
- **معاينة Vercel محمية بمصادقة**؛ تُفتح بحساب المالك فقط.
- **حقل الجسيمات لم يُقَس على جهاز جوال حقيقي.** التحقّق كان بـ Chromium
  headless محلي (viewport محاكى + `prefers-reduced-motion`)، لا FPS فعلي على
  عتاد ضعيف. الكثافة صارت تتبع المساحة (91 عقدة عند 375×700 بدل 400)، فالحمل
  على الجوال انخفض كثيراً عمّا كان — لكنه ما زال غير مقيس على عتاد حقيقي. راجع
  معدل الإطارات على جهاز فعلي، وإن كان ضعيفاً فالخيار عرضه على المالك لا
  تخفيفه تلقائياً.
