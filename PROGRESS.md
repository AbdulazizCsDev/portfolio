# PROGRESS.md

الفرع: `redesign/ink-and-gold` · مبني على `main` عند `0f1a321` · **لا تدفع على `main`**
`npm run lint` ✅ · `npm run build` ✅ · معاينة حيّة تُبنى تلقائياً من كل دفعة على الفرع

إعادة تصميم كاملة وفق `docs/DESIGN-SPEC.md`. نُفِّذت على سبع مراحل، كل واحدة
تنتهي ببناء ناجح وموقع يعمل.

## تم

- **طبقة الرموز ونظام الوضعين** — لوحة الحبر والذهب في `:root`، بقيمتين لنفس
  الصبغة حتى يصمد الذهب على الورق. الوضع في `data-mode` على `<html>`، يُختم قبل
  أول رسم بسكربت مضمّن في `index.html`، ويملكه `ThemeContext` بعدها.
- **استخراج كل قيمة لونية خام** — 82 في CSS وأكثر في JSX (الكانفس، خامات WebGL،
  تدرّجا SVG، أزرق لينكدإن). بقي استثناء واحد مقصود: قيمة احتياطية في
  `TraceField.jsx:37` تُستعمل لو لم تُحلّ رموز CSS بعد.
- **تصفية الممنوعات** — حُذفت الزجاجية والتدرجات وظلال التوهّج. الخطوط من ستة
  أوزان إلى وزنين، والاستيراد من 15 وجهاً إلى 5.
- **خصائص منطقية** — كل `left`/`right` صارت `inset-inline-*`، فسقطت ست كتل
  تجاوز RTL كانت تكرّر التخطيط يدوياً.
- **موبايل أولاً** — 27 استعلام `max-width` صارت 12 استعلام `min-width`. ست شبكات
  حُذفت استعلاماتها كلياً لصالح `repeat(auto-fit, minmax(min(300px, 100%), 1fr))`.
- **العربية RTL هي الافتراضي**، و`Cairo` هو الخط الأساس. الإنجليزية لغة ثانية كاملة.
- **الحركة بالوزن** — `--t-fast/base/heavy` بدل متغيّري `all`، و23 موضع استدعاء
  أُعيدت كتابتها بخصائص صريحة. لا شيء يحرّك خاصية تخطيط.
- **بطاقات المشاريع** — من سبعة مشاريع إلى ثلاثة، وكل بطاقة أربعة عناصر بالضبط:
  الاسم، الرقم الواحد وسطر يفسّره، الملخص، سطر "ما انكسر". صفر وسوم وصفر أسطح
  قابلة للتوسيع.
- **عزل ثنائي الاتجاه** — `src/lib/Bidi.jsx` يلفّ المصطلحات اللاتينية والأرقام
  داخل الجمل العربية في `<bdi>`؛ 34 موضع استدعاء تنتج 99 عازلاً في الصفحة.
- **صفحات المشاريع** — توجيه بمسارات حقيقية (`/project/<id>`) في 53 سطراً على
  History API (`lib/router.js` + `lib/Link.jsx`)، مع `vercel.json` يعيد كتابة كل مسار إلى `index.html`.
- **تسلسل التعاير** — الشاشة الحاجبة (3.1 ثانية) حُذفت. الصفحة تصل كاملة ومزاحة
  قليلاً، ستة عناصر تتقارب عبر 2200ms، ومؤشّر `calibrating` → `stable` يخفت.
  مرة واحدة لكل جلسة عبر `sessionStorage`، وملغى كلياً مع `prefers-reduced-motion`.
- **الخلفية** — `TraceField` بديلاً عن `NeuralBackground` و`NeuralLogo3D`. حُذفت
  تبعية `three` ومعها الشعار ثلاثي الأبعاد؛ الحزمة من 765KB على قطعتين إلى 253KB.
- **الوصولية** — حلقة تركيز على كل عنصر تفاعلي، رابط تخطٍّ، ومعالجة فيض بطاقات
  التواصل عند 320px.

**قياسات محقّقة:** 157 عنصراً نصياً يعبر 4.5:1 في الوضعين · صفر تجاوز أفقي عند
320/768/1280 · كثافة حبر الخلفية 0.35%→0.96% (السقف 5%) · صفر إطار بعد استقرار
الخلفية أو عند إخفاء التبويب · حذف `<TraceField />` من `App.jsx` يترك الموقع كاملاً.

## قيد العمل

لا شيء قيد التنفيذ. المراحل السبع مدموجة في الفرع وشجرة العمل نظيفة.

آخر ملف مُس: `PROGRESS.md` و`CLAUDE.md` (هذا التحديث). لا دالة نصف مكتوبة ولا
تعديل معلّق في أي مكان.

## قرارات

- **مسارات حقيقية بدل راوتر hash.** الاثنان صفر تبعيات. رُفض الـ hash لأن صفحات
  المشاريع هي أكثر ما يستحق المشاركة والفهرسة، وروابط `#/` تُفهرس بضعف. الكلفة
  الزائدة: rewrite واحد في `vercel.json`.
- **رُفض `react-router`** — §1 في المواصفة يمنع أي حزمة جديدة، والتوجيه المطلوب
  بسيط بما يكفي (55 سطراً).
- **`--ease-settle` غُيِّر عن حرفية المواصفة.** المنحنى المكتوب في §2 يعطي تجاوزاً
  0.13% بينما §4 يطلب 2–3%. المواصفة تناقض نفسها؛ اعتُمد
  `cubic-bezier(0.22, 1.28, 0.36, 1)` = 2.5% بقرار المالك.
- **سطر "ما انكسر" بـ`--text-muted` لا `--text-dim`.** §7 يحدّد `--text-dim`، لكنه
  يعطي 2.79:1 على سطح البطاقة، وهذا السطر يحمل أطروحة الموقع. §9 (4.5:1) يتقدّم.
  `--text-muted` = 5.97:1 وما زال أهدأ من الملخص.
- **الوسوم التقنية حُذفت من البطاقات** لا نُقلت — §7 يمنعها، ووجودها في مكان آخر
  يعيد المشكلة نفسها.
- **الخلفية تتوقف عند استقرارها** ولا تعيد البذر بلا نهاية. رُفض التكرار الدائم
  لأنه أشبع اللوحة خلال ثوانٍ ودفن النص. ورُفض التوقّف المطلق أيضاً لأنه ألغى
  تدرّج §6 (أسفل الصفحة كان يعرض رسم الأعلى). الحل: بذر على دفعات عند انتقال طور
  السكرول، تحت سقف صارم للمقاطع.
- **الوضع يُحفظ عند النقر فقط** لا عند التركيب — الحفظ عند التركيب يجعل كل زائر
  يبدو وكأنه اختار، فيتعطّل تتبّع تفضيل النظام.
- **إعادة بناء الخلفية عند تبديل الوضع** — الخطوط المرسومة على الكانفس لا يمكن
  إعادة تلوينها، فالحقل يبدأ من جديد.
- **لا اختبارات آلية.** مشروع واجهة عرض؛ التحقّق عبر `lint` + `build` + قياس في
  المتصفح. القياس في المتصفح ليس اختياريّاً هنا: عدة أخطاء لم تظهر إلا فيه.

## التالي

**تعبئة المحتوى المتروك للمالك.** الملف `src/context/LanguageContext.jsx` يحوي
**60 نصاً نائباً** معلَّماً بـ`[[ … ]]`، كلها في كائني `en` و`ar` معاً. الخطوة:

1. لكل مشروع من الثلاثة (`board-room`, `aime`, `agrocure`) في `projects.items`:
   - `number` — الرقم الأصعب أو الأغرب، لا الأكبر
   - `numberNote` — سطر يقول ماذا يقيس
   - `summary` — جملة أو جملتان عمّا يفعله المشروع، لا التقنيات فيه
   - `broke` — ما الذي لم ينجح وماذا تعلّم منه
   - `detail.sections[]` — ستة أقسام: المعمارية، القرارات، المقاييس، الحدود،
     ما جُرّب وفشل، الكود
2. أي نص يُكتب في `en` يُكتب في `ar` — البنيتان مرآة، والنقص يكسر التبديل.
3. بعد التعبئة: `npm run lint && npm run build`، ثم فحص الصفحة الرئيسية وصفحة
   مشروع واحدة على الأقل في الوضعين وعند 320px.

هذا العمل لا يحتاج قراراً إضافياً من أحد — المواضع محدّدة والشروط مكتوبة داخل
النص النائب نفسه.

## مشاكل معروفة

- **`README.md` ما زال قالب Vite الافتراضي** ولا يصف المشروع.
- **`src/assets/react.svg` و`vite.svg` و`hero.png` غير مستوردة** في أي مكان.
- **المساعد يعتمد على خدمة خارجية** (`AimeWidget.jsx`، عنوان ثابت في الكود). إن
  سقطت يظهر نص اعتذار فقط، ولا يوجد وضع بديل. العنوان يستحق أن يصير `VITE_*`.
- **`LanguageContext.jsx` 522 سطراً** يخلط المحتوى بالمزوّد.
- **`AimeWidget.jsx` 604 أسطر** ولم يُراجَع ضمن إعادة التصميم إلا لونياً وحركياً؛
  بنيته الداخلية كما كانت.
- **لا اختبارات** — أي انحدار يُكتشف يدوياً.
- **المعاينة على Vercel محمية بمصادقة**؛ تُفتح بحساب المالك فقط.
