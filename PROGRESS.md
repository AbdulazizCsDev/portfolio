# PROGRESS.md

آخر تحديث: 2026-08-24 · الفرع: `claude/create-claude-progress-docs-70lvda` · شجرة العمل نظيفة
`npm run lint` ✅ · `npm run build` ✅ (43 وحدة، 1.20s)

## تم
- الموقع كامل ومنشور على Vercel: هيرو، نبذة، خبرة، مشاريع، قيد البناء، مهارات، تواصل.
- ثنائية اللغة EN/AR كاملة مع RTL، والتبديل فوري بدون إعادة تحميل.
- شاشة افتتاحية (رسم الشعار ثم التلاشي) وشعار ثلاثي الأبعاد ثابت خلف المحتوى.
- **Aime** — مساعد نصي/صوتي: تسجيل بكشف نشاط صوتي (VAD) يوقف تلقائياً عند الصمت،
  تحويل صوت→نص، ردّ من الخادم، ثم نطق الرد، مع كتم وتوسيع للوحة.
- Aime يقود الصفحة: يمرّر للأقسام، يضيء بطاقة مشروع بعينها، إجابات جاهزة بدون
  رحلة للـ LLM، وجولة مُرشدة كاملة بالصوت.
- إعادة تموضع المحتوى كامل من "مطوّر" إلى **مهندس ذكاء اصطناعي / تعلّم آلة**.
- روابط Live وGitHub لمشاريع AI Board Room وAgroCure، وتحديث بيانات مشروع Aime.
- إصلاحات تخطيط: اللوحة تزيح المحتوى بدل تغطيته، والصفحة تبدأ دائماً من الأعلى.
- تنظيف كود ميت: حذف `ThreeScene.jsx` (غير مستورد إطلاقاً) والمراجع الميتة
  `pendingGreetRef` و`bubbleTextRef` في `AimeWidget` (كانت تُكتب ولا تُقرأ).

## قيد العمل
- لا شيء قيد التنفيذ حالياً — آخر دفعة عمل مدموجة في `main` (PR #3).
- هذا الفرع يحمل التوثيق (`CLAUDE.md` + `PROGRESS.md`) وتنظيف الكود الميت.

## قرارات
- **الترجمة هي طبقة البيانات**: المشاريع والمهارات والشهادات داخل
  `LanguageContext` بدل ملف بيانات منفصل — يضمن أن أي محتوى جديد يُكتب باللغتين.
- **الخادم يقود التنقّل**: يرجّع `action` مثل `projects.board-room`؛ و`detectIntent`
  المحلي احتياطي فقط عند غياب الحقل.
- **إجابات جاهزة** للأسئلة المتكرّرة (المشاريع، التواصل) — الصفحة نفسها هي
  المحتوى، فلا داعي لتكلفة وتأخير نداء الـ LLM.
- **VAD بدل الضغط المطوّل** للتسجيل: نقرة للبدء وتوقف تلقائي عند الصمت.
- **three ديناميكي** مع حارس `prefers-reduced-motion` — الحزمة الرئيسية تبقى خفيفة.
- **لا TypeScript ولا اختبارات**: مشروع واجهة عرض بحجم صغير، والتحقق عبر lint+build.
- **لا مكتبة UI**: نظام تصميم صغير بمتغيّرات CSS يحافظ على الطابع البصري الخاص.

## التالي
- استبدال `README.md` (ما زال قالب Vite الافتراضي) بوصف حقيقي للمشروع.
- إضافة رابط "الخبرة" لقائمة الـ Navbar — القسم موجود ومتتبَّع لكن غير قابل للنقر.
- نقل عنوان خادم Aime إلى `VITE_*` بدل ثابت مكتوب في الكود.
- تقسيم `LanguageContext.jsx` (538 سطر) لو كبر المحتوى أكثر: محتوى منفصل عن المزوّد.

## مشاكل معروفة
- تحذير بناء: حزمة `three` ‏508KB (127KB مضغوطة) — مقبول حالياً لأنها مؤجّلة التحميل.
- المساعد يعتمد كلياً على خدمة خارجية منفصلة؛ إن سقطت يظهر نص اعتذار فقط،
  ولا يوجد وضع بديل بلا صوت.
- لا اختبارات إطلاقاً — أي انحدار يُكتشف يدوياً في المتصفح.
- `Particle.reset` في `NeuralBackground` يحمل فرعي `randomY` متطابقين (شرط بلا أثر).
- بيانات السيرة/الخبرة/التواريخ مكتوبة يدوياً في مكانين (EN/AR) — عرضة للتباعد.
- `t.aime.bubbleGreet` بقي في `translations` (EN/AR) بلا مستهلك بعد حذف
  `bubbleTextRef` — نص فقاعة ترحيب غير معروض؛ يُحذف أو يُستعمل، قرار محتوى.
